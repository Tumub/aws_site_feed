# Updated deploy_ec2.yml

# Fixing environment variable expansion issues

# Variables
HOST="${HOST}"
USER="${USER}"

# SCP command
scp -r -i /path/to/key ${USER}@${HOST}:/source/path /destination/path

# SSH command
ssh -i /path/to/key ${USER}@${HOST}