import { Truck } from "lucide-react";
import { ExpertiseLayout } from "@/components/layout/ExpertiseLayout";

const FlowExpertise = () => {
  return (
    <ExpertiseLayout 
      pillar="flow" 
      icon={Truck} 
      accentColor="amber"
    />
  );
};

export default FlowExpertise;
